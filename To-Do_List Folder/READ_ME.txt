***instructions***

Before running the make sure you have the latest python version or at least a version greater than 3.9
Then open To-Do-List.py file paste the path to the directory in which you'll be storing the Lists in code manually on LINE 18 paste it properly inside "" the correct path or the code will show errors after execution to open the To-Do-List.py you may use any code editors you have on your Device.
 
And if you don't then you can also open it using notepad but remember it can be a little trickier since the code won't appear properly and it also depends which version of notepad your using.